export const LIMITS = Object.freeze({
  maxAudioBytes: 40 * 1024 * 1024,
  maxImageBytes: 12 * 1024 * 1024,
  maxProjectBytes: 64 * 1024 * 1024,
  warningProjectBytes: 40 * 1024 * 1024,
  maxCues: 200
});

export const DEMO_CONFIG = Object.freeze({
  maxImages: 5
});
